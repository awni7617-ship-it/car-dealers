{
  "name": "forecourt",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dealership stock, enquiry and diary tracker \u2014 Cloudflare Workers + D1.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply forecourt --local",
    "migrate": "wrangler d1 migrations apply forecourt --remote",
    "standalone": "node scripts/build-standalone.mjs",
    "test": "node --test \"tests/**/*.test.js\"",
    "check": "node scripts/check.mjs",
    "deploy:cf": "CI=true wrangler d1 migrations apply forecourt --remote && CI=true wrangler deploy",
    "standalone:site": "node scripts/build-standalone.mjs && mkdir -p dist-static && cp forecourt.html dist-static/index.html"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
